// String 
let city:string = 'Chisinau';

// Number
 let num:number = 2;

// boolean
let isRaining:boolean = true;
    isRaining = false;

// Unknown
let value: number | string; 
value =  'Name';
value = 2020;
//console.log(value);

// Array 
let numArray: number[] = [1,2,3,5,2];
let numArray1: Array<number> = [2,4,6];

let strArray: string [] = ['Madrid', 'Praga'];
let strArray1: Array<string> = ['London', 'Berlin'];

let boolArray: boolean[] = [true, false];
let boolArray1: Array<boolean> = [false, true];



/*  Tuples
* - The number of elements of the array is fixed.
* - The type of the elements is known.
* -The type of the elements of the array need not be the same.
*/

let array: [number, string] = [10, 'Kiev'];

let arrayOfTuples: [string, number][] = [['Dan', 20], ['Ion', 31], ['Macu', 19], ['Andrei', 26]];










